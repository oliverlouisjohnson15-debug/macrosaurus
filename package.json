{
  "name": "macrosaurus",
  "version": "1.0.0",
  "private": true,
  "description": "Adaptive UK macro and body-composition tracker (PWA).",
  "scripts": {
    "test": "node --experimental-strip-types --disable-warning=ExperimentalWarning --test tests/*.test.js",
    "calibrate": "node tools/calibrate.mjs",
    "build:calibration": "node tools/build-calibration.mjs"
  },
  "dependencies": {
    "@babel/core": "^8.0.1",
    "@babel/preset-react": "^8.0.1",
    "tailwindcss": "^3.4.1"
  }
}
